[project]
name = "tools"
version = "0.1.0"
description = "XAI Pipeline for Lost In Time DDA"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
    "shap>=0.45",
    "llvmlite>=0.43.0",
    "onnx>=1.15",
    "onnxruntime>=1.17",
    "matplotlib>=3.7.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
